#!/usr/bin/env bash

# Usage:
#
#     $ bin/release <build-dir>
echo "##################### release"

BUILD_DIR=$1

BIN_DIR=$(cd $(dirname $0); pwd) # absolute path

cat << EOF
---
default_process_types:
EOF
